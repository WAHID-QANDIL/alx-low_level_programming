#include "main.h"
/**
 * mul - this fun take two parameters and return the mul
 *
 * @a: number one
 * @b:number two
 * Return: return the multiple of these numbers (a & b)
 */
int mul(int a, int b)
{
	return (a * b);
}
