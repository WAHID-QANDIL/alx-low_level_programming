#include"main.h"
/**
 * _abs - return absolute value for n
 * @n: input function
 * Return: return abs for n if n < 0 alse return n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
